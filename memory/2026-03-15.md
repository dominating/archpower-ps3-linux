
## 2026-03-15 (Entropy ESP32-S2 Firmware Compilation)
- Compiled the `entropy-zero-fw` firmware for the ESP32-S2 architecture.
- Used dummy OpenSSL certificates (`cert_pem.h`, `private_key.h`, `root_crt.h`) to bypass the build errors caused by missing official community certificates.
- Uploaded the compiled binaries (`ENTROPY_FW.bin`, `bootloader.bin`, `partition-table.bin`) to the Vercel deployment at `depin-hub.vercel.app/firmware/s2/`.
- **Key finding**: Entropy enforces hardware lock-in using mTLS on their MQTT broker. While the dummy certificates allow the firmware to compile and boot, the MQTT server rejects the self-signed certificates during the TLS handshake, making it impossible to mine the memecoin without official keys from their portal.
- **User Context**: Solana Address for the Entropy miner is `[REDACTED]`.
- **DuinoCoin Miner**: Ported `cifertech/DucoMiner-Monitor-ESP32` to the ESP32-S2 platform, removed all OLED monitor/Adafruit dependencies for headless operation, injected the user's Solana address (`[REDACTED]`) into the DuinoCoin config, and compiled the `merged.bin`. Available on Vercel at `/firmware/s2/duco/`.
- **DuinoCoin Miner**: Recompiled the headless DuinoCoin miner per user request with updated config (`wifi_ssid = "Snug as a Bug"`, `wifi_password = "Loop2loop@1337"`, `username = "micemeat"`) and re-deployed the `.bin` files to Vercel.
- **Cleanup**: Recompiled DuinoCoin miner to use generic "YOUR_WIFI_SSID" and "YOUR_WIFI_PASSWORD" instead of the user's personal credentials. Deleted the entire Entropy firmware codebase (`entropy-zero-fw`) and removed its compiled bins from the Vercel deployment since it's impossible to mine without proper mTLS certs.
- **System Action**: Performed nightly memory consolidation. Extracted Entropy mTLS lock-in findings and DuinoCoin headless port details into `MEMORY.md`.
