# Daily Note: 2026-03-28

**Work Completed:**
- **DePIN Hub (PassivePin):**
  - Went live with a custom domain! The hub is now officially hosted at `passivepin.xyz` (registered via Spaceship) and DNS is successfully pointed to Vercel.
  - Added GoMining and NodeGo AI to the directory.
  - Wrote a Node.js script (`shorten.js`) to programmatically shorten all external links on the site using the `cryptol.ink` API (handling rate limits gracefully).
- **PS3 Remote Play Port (`libps3rp`):**
  - Successfully compiled the standalone `open-rp` binary on Linux after replacing legacy FFmpeg calls (`avcodec_decode_video2`) with modern `libavcodec` APIs.
  - Stripped the Linux-specific UI bloat (wxWidgets, SDL) from the C++ core and isolated the authentication, crypto, and network logic into a clean `libps3rp.so` shared library.
  - Drafted the initial JNI stubs (`ps3rp_jni.cpp`) to prepare for Android/Chiaki-NG integration using MediaCodec.
- **PS3 Homebrew Revival (Super Mario War):**
  - Spun up a dedicated PS3-Homebrew-Dev sub-agent to finish stalled PS3 homebrew ports and recreate canceled titles.
  - Cloned the legacy Super Mario War (`SMW-PS3`) codebase.
  - Ripped out legacy PSL1GHT pad polling (`<io/pad.h>`) and replaced the SDL 1.2 initialization with modern SDL2 hardware-accelerated rendering equivalents in `main.cpp` and `splash.cpp`.
  - Created a `setup_env.sh` script for building the modern open-source PS3 toolchain and SDL2 libraries.

**Consolidation Summary:**
- Nightly memory consolidation completed. Updated `MEMORY.md` to track the successful `passivepin.xyz` launch, `libps3rp` JNI isolation, and the start of the `PS3 Homebrew Revival` project for Super Mario War.