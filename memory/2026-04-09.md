# Daily Note: 2026-04-09

**Work Completed:**
- Scaffolded the `ps3-xnu-darwin` repository in the workspace to begin porting the Apple XNU Kernel (Mac OS X 10.5 Leopard era) to run bare-metal on the PS3 Cell Broadband Engine.
- Wrote the initial `ARCHITECTURE.md` explaining the jump from Petitboot to XNU.
- Built the `lv1_calls.h` hypervisor interface (using inline PowerPC `sc 1` assembly) to allow XNU to request storage, memory, and console output directly from the Sony Lv1 Hypervisor.
- Created `ps3_boot.c`, the wrapper that initializes the memory map before passing execution to the XNU Mach kernel.
- Implemented `PE_ps3.c` to act as the Apple Platform Expert (replacing Mac OpenFirmware). This component mocks the device tree and routes XNU's early `kprintf` functions into the Lv1 Hypervisor console.
- Wrote `fetch_xnu.sh` to automatically pull down the open-source Apple `xnu-1228` (OS X 10.5) source code for patching.
**Memory Consolidated:** Nightly consolidation executed for 2026-04-08. Wrote PS3 Cell Media scaffolding and DePIN giveaways/nodes to MEMORY.md.
**Consolidation Summary (2026-04-10):**
- Extracted and updated MEMORY.md with details on the ps3-xnu-darwin Apple XNU port for PS3 and the Gamma Bitaxe 601 affiliate link integration on the DePIN Hub.
