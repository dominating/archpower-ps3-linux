# Daily Note: 2026-04-05

**Work Completed:**
- **PassivePin / DePIN Hub Redesign:**
  - Separated the PS3 Homebrew and ESP32 Firmware/Hardware guides from the main `index.html` page into a dedicated `hardware-projects.html` page.
  - Reorganized the core DePIN directory on `index.html` into three clean categorized tables: **Browser Extensions**, **Mobile & Bandwidth Apps**, and **DePIN Nodes & Hardware**.
  - Wrote a Node.js script (`parse_and_rebuild.js`) to parse the raw HTML tables, group entries, inject new table wrappers with colored borders, and rewrite the HTML files safely.
  - Successfully deployed the changes live to Vercel via `vercel --prod --yes`.